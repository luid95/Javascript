'use strict'
window.addEventListener("load", function(event) {
  //Bucle for
  //Bucle es una estructura de control que se repite varias veces

  var numeros = 100;

  for (var i = 0; i < numeros; i++) {
    console.log("Vamos por el numero "+ (i+1));
  }
});
